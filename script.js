var apikey ="";
var cities=[];



/*
function displayWeather() {
    var city = $("#text").val().trim();
    var queryURL = "https://------------=" + cty + "&appid" + append
    $ajax({
        url: queryURL,
        method: "GET"
    }).then(function(response){
        var ### = response.weather[0].icon;
        var ### "http:/--------" + --- + ".png";

        $(".name").text(response.name);
        $(".date").text(new Date(response.dt*1000).toLocaleDateString();)
    })

}
function userindex( #33, let) {()
    var userindex
});
*/